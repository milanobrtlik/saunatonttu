import 'package:flutter/material.dart';

class BuddiesManageView extends StatelessWidget {
  const BuddiesManageView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
